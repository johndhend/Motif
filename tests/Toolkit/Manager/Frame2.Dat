#) A client window will appear with a rowColumn widget which
   contains three frame widgets.  The top frame widget has an
   arrowButton child, and the bottom two frame widgets have
   pushbutton children. 
#) Move the client window so that it partially obscures the term
   window.  Raise the term window.  Raise the client window. 
C)
#) The top frame now has a marginWidth of 20, a marginHeight of 20,
   and shadowType of XmSHADOW_ETCHED_OUT. 
C)
#) The top frame has now disappeared. 
E)
